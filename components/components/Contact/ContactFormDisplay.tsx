import { FC } from "react";
import styled from "styled-components";
interface CFDProps {}

const ContactFormDisplay: FC<CFDProps> = ({}) => {
  return <div>ContactFormDisplay</div>;
};

export default ContactFormDisplay;
