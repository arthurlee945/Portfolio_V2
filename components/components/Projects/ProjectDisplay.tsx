import { FC } from "react";
import styled from "styled-components";

interface ProjectDisplayProps {}

const ProjectDisplay: FC<ProjectDisplayProps> = ({}) => {
  return <section>ProjectDisplay</section>;
};

export default ProjectDisplay;
